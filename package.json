{
  "name": "urd",
  "version": "0.5.0",
  "private": true,
  "description": "URD's Roll Derivation — Verifiable Randomness for Nostr Games",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test src/index.test.ts src/sra.test.ts src/deck.test.ts"
  },
  "devDependencies": {
    "typescript": "^6",
    "@types/node": "^24"
  }
}
