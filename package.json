{
  "name": "urd",
  "version": "0.1.0",
  "private": true,
  "description": "URD's Roll Derivation — Verifiable Randomness for Nostr Games",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
